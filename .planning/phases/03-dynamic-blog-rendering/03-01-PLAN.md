---
phase: 03-dynamic-blog-rendering
plan: 01
type: execute
---

<objective>
Convert blog listing and detail pages from static TypeScript imports to dynamic Supabase queries while preserving all URLs, SEO metadata, and rendering behavior.

Purpose: Enable dynamic blog content management without breaking existing URLs or search rankings.
Output: Blog pages fetching from Supabase database with identical user experience to static implementation.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@src/app/blog/page.tsx
@src/app/blog/[slug]/page.tsx
@src/data/blog-posts.ts
@src/types/blog.ts
@src/lib/supabase/server.ts

**Tech stack available:**
- Next.js 16 App Router with Server Components
- Supabase SSR client (`createServerClient`)
- TypeScript strict mode
- Existing BlogPost type interface

**Established patterns:**
- Server Components for data fetching
- Supabase service role for admin operations
- Metadata generation via `generateMetadata()`
- Static params generation via `generateStaticParams()`

**Constraining decisions:**
- Keep existing BlogPost schema structure (from Phase 1)
- Maintain existing URLs (`/blog/[slug]`)
- Preserve SEO metadata and structured data
- Use slug field for routing (not database IDs)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update blog listing page to fetch from Supabase</name>
  <files>src/app/blog/page.tsx</files>
  <action>
Replace static import of `blogPosts` array with Supabase query:

1. Import `createServerClient` from `@/lib/supabase/server`
2. Create Supabase client in the component (Server Component pattern)
3. Query `blog_posts` table:
   - Select: slug, title, excerpt, category, published_at, read_time, featured_image, featured_image_alt, tags
   - Order by: published_at DESC (newest first)
   - Use `.from('blog_posts').select('...')` pattern
4. Map Supabase response to match existing BlogPost interface:
   - published_at → publishedAt (camelCase)
   - read_time → readTime
   - featured_image → featuredImage
   - featured_image_alt → featuredImageAlt
5. Keep all existing rendering logic (category pills, grid layout, CTA) unchanged
6. Handle error states: if query fails, show error message "Failed to load blog posts"

AVOID using `blogPosts` import from `src/data/blog-posts.ts` - this is the static data we're replacing.

Use server-side Supabase client (not browser client) since this is a Server Component.
  </action>
  <verify>
1. npm run build succeeds without TypeScript errors
2. Navigate to /blog in dev mode - should show 3 posts
3. Check browser console - no errors
4. Verify posts ordered by date (newest first)
  </verify>
  <done>
Blog listing page fetches from Supabase, displays all 3 posts in correct order, no errors, build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Update blog detail page to fetch from Supabase</name>
  <files>src/app/blog/[slug]/page.tsx</files>
  <action>
Replace static `getBlogPostBySlug()` import with Supabase query:

1. Import `createServerClient` from `@/lib/supabase/server`
2. In the main page component, create Supabase client
3. Query single post:
   - `.from('blog_posts').select('*').eq('slug', params.slug).single()`
   - This fetches by slug (not ID) to maintain URL routing
4. Map database fields to camelCase for rendering:
   - published_at → publishedAt
   - updated_at → updatedAt
   - read_time → readTime
   - featured_image → featuredImage
   - featured_image_alt → featuredImageAlt
   - meta_title → metaTitle
   - meta_description → metaDescription
   - target_keyword → targetKeyword
   - related_neighborhoods → relatedNeighborhoods
   - related_posts → relatedPosts
   - related_services → relatedServices
5. If post not found, return `notFound()` (Next.js 404 handler)
6. Update `generateStaticParams()`:
   - Query Supabase for all slugs: `.from('blog_posts').select('slug')`
   - Return array of `{ slug }` objects for static generation
7. Update `generateMetadata()`:
   - Use same Supabase query pattern to fetch post
   - Keep existing metadata structure (metaTitle, metaDescription, tags, OpenGraph)
8. Keep all rendering logic unchanged (markdown content, related posts, social share)

AVOID using `getBlogPostBySlug()` or `getAllBlogSlugs()` from `src/data/blog-posts.ts`.

Use `notFound()` from `next/navigation` for 404 handling (not custom error page).
  </action>
  <verify>
1. npm run build succeeds
2. Navigate to /blog/how-to-sell-home-fast-kansas-city - should render full post
3. Check page source - verify SEO metadata present (title, description, Open Graph)
4. Navigate to /blog/invalid-slug - should show 404
5. Verify all 3 posts accessible via their slugs
  </verify>
  <done>
Blog detail pages fetch from Supabase by slug, render full content with SEO metadata, 404 for invalid slugs, all 3 posts accessible
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run dev` starts without issues
- [ ] Blog listing page (/blog) shows 3 posts
- [ ] Each blog post accessible via slug (/blog/[slug])
- [ ] SEO metadata visible in page source (metaTitle, metaDescription)
- [ ] Invalid slug shows 404 page
- [ ] No TypeScript errors in blog pages
</verification>

<success_criteria>

- Both blog pages (listing and detail) fetch from Supabase
- All 3 migrated posts render correctly
- URLs unchanged (/blog and /blog/[slug])
- SEO metadata preserved (title, description, OpenGraph)
- 404 handling works for invalid slugs
- No errors in build or runtime
- Static data imports removed from blog pages
</success_criteria>

<output>
After completion, create `.planning/phases/03-dynamic-blog-rendering/03-01-SUMMARY.md`:

# Phase 3 Plan 1: Blog Rendering Migration Summary

**Converted blog pages from static TypeScript to dynamic Supabase queries**

## Accomplishments

- Blog listing page now fetches from Supabase database
- Blog detail pages query by slug while maintaining URLs
- SEO metadata generation updated for database-driven content
- Static params generation uses Supabase for build-time optimization
- Error handling added (404 for invalid slugs)

## Files Created/Modified

- `src/app/blog/page.tsx` - Updated to fetch posts from Supabase
- `src/app/blog/[slug]/page.tsx` - Updated to fetch single post by slug

## Decisions Made

- Use Server Components pattern for data fetching (no client-side queries)
- Map database snake_case to camelCase for consistency with existing types
- Maintain generateStaticParams() for build-time optimization (not fully dynamic)
- Use notFound() for invalid slugs (Next.js standard pattern)

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 03-02-PLAN.md (Sitemap + SEO verification)
</output>
