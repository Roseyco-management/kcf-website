---
phase: 04-cms-foundation
plan: 01
type: execute
---

<objective>
Build admin dashboard structure at `/admin/blog` with navigation, layout, and protected routes to serve as foundation for blog management CMS.

Purpose: Create the scaffolding for blog management before building list/edit interfaces in subsequent phases.
Output: Working `/admin/blog` route with navigation, auth protection, and empty state ready for blog features.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/03-dynamic-blog-rendering/03-01-SUMMARY.md
@.planning/phases/03-dynamic-blog-rendering/03-02-SUMMARY.md
@src/app/admin/layout.tsx
@src/app/admin/page.tsx
@src/components/admin/admin-header.tsx

**Tech stack available:**
- Next.js 16 App Router with Server Components
- Supabase auth (already integrated in admin layout)
- Tailwind CSS 4 for styling
- Lucide React icons
- Existing admin dashboard at `/admin`

**Established patterns:**
- Admin layout with AdminHeader component and auth guards
- Server Components for data fetching
- Double-quote strings, 2-space indentation
- PascalCase for components, camelCase for utilities

**Constraining decisions:**
- Admin CMS in existing `/admin` dashboard (reuse existing auth and layout)
- Keep existing BlogPost schema structure
- Supabase database already has blog_posts table with all fields
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /admin/blog page with navigation layout</name>
  <files>src/app/admin/blog/page.tsx</files>
  <action>
Create Server Component at `/admin/blog` that serves as the blog management dashboard:

1. Export default async function `BlogManagementPage()`
2. Add heading "Blog Management" with subtitle "Manage blog posts, create new content, and publish"
3. Create navigation tabs/sections for future features:
   - "All Posts" - will show blog list (Phase 5)
   - "Create New" - will open editor (Phase 6)
   - "Drafts" - placeholder for draft management
   - "Published" - placeholder for published filter
4. Each section should be a placeholder card with:
   - Icon from lucide-react (FileText, PlusCircle, FileClock, CheckCircle)
   - Title
   - Brief description ("Coming soon: View and edit all blog posts")
   - Disabled state styling (opacity-50, cursor-not-allowed)
5. Use grid layout: `grid grid-cols-1 md:grid-cols-2 gap-6`
6. Follow existing admin page styling patterns (rounded-2xl cards, white backgrounds, border colors)
7. Use navy (#151A4A) for headings, gold (#C9A961) for accents

AVOID creating actual blog list or editor - those are Phase 5 and 6. This is just scaffolding.

Use Tailwind classes matching the existing admin dashboard style (check @src/app/admin/page.tsx for patterns).
  </action>
  <verify>
1. npm run dev starts without errors
2. Navigate to http://localhost:3000/admin/blog (requires login)
3. Verify 4 placeholder cards render in grid
4. Verify heading and subtitle present
5. Check TypeScript compilation: no errors
  </verify>
  <done>
/admin/blog route exists, renders 4 placeholder cards, auth-protected, no errors, matches existing admin styling
  </done>
</task>

<task type="auto">
  <name>Task 2: Add blog navigation link to admin header</name>
  <files>src/components/admin/admin-header.tsx</files>
  <action>
Update AdminHeader component to include navigation link to blog management:

1. Import Link and usePathname from Next.js
2. Add "use client" directive if not already present
3. Between logo and user menu, add navigation links section:
   ```tsx
   <nav className="flex items-center gap-6">
     <Link
       href="/admin"
       className={`text-sm font-medium transition-colors hover:text-[#C9A961] ${
         pathname === '/admin' ? 'text-[#151A4A]' : 'text-[#4A4A4A]'
       }`}
     >
       Dashboard
     </Link>
     <Link
       href="/admin/blog"
       className={`text-sm font-medium transition-colors hover:text-[#C9A961] ${
         pathname?.startsWith('/admin/blog') ? 'text-[#151A4A]' : 'text-[#4A4A4A]'
       }`}
     >
       Blog
     </Link>
   </nav>
   ```
4. Use `usePathname()` hook to detect active route
5. Active link should be navy (#151A4A), inactive should be gray (#4A4A4A)
6. Hover state should be gold (#C9A961)

AVOID adding more navigation items - just Dashboard and Blog for now. Keep it simple.

The header is already a client component (it has UserMenu), so "use client" should already be present.
  </action>
  <verify>
1. Visit /admin - "Dashboard" link is active (navy color)
2. Visit /admin/blog - "Blog" link is active (navy color)
3. Hover over links - color changes to gold
4. Both links clickable and navigate correctly
5. No console errors
  </verify>
  <done>
Admin header has Dashboard and Blog navigation links, active state highlighting works, hover effects present, navigation functional
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run dev starts without errors
- [ ] /admin/blog route accessible (requires auth)
- [ ] Blog management page shows 4 placeholder cards
- [ ] Admin header shows Dashboard and Blog nav links
- [ ] Active route highlighting works
- [ ] No TypeScript errors
- [ ] Styling matches existing admin dashboard
</verification>

<success_criteria>

- /admin/blog route created and auth-protected
- Page shows placeholder cards for future features (All Posts, Create New, Drafts, Published)
- Admin header includes navigation to Dashboard and Blog
- Active route highlighting functional
- All styling matches existing admin patterns
- No errors in build or runtime
- Foundation ready for Phase 5 (blog list) and Phase 6 (editor)
</success_criteria>

<output>
After completion, create `.planning/phases/04-cms-foundation/04-01-SUMMARY.md`:

# Phase 4 Plan 1: CMS Foundation Summary

**Created admin blog management dashboard scaffolding with navigation**

## Accomplishments

- /admin/blog route created with auth protection
- 4 placeholder cards for future features (All Posts, Create New, Drafts, Published)
- Admin header updated with Dashboard and Blog navigation
- Active route highlighting implemented
- Foundation ready for blog list and editor features

## Files Created/Modified

- `src/app/admin/blog/page.tsx` - Blog management dashboard (new)
- `src/components/admin/admin-header.tsx` - Added blog navigation link

## Decisions Made

- Use placeholder cards to show planned features without implementing them yet
- Keep navigation simple (Dashboard, Blog only)
- Match existing admin dashboard styling patterns for consistency
- Auth protection inherited from /admin layout (no additional guards needed)

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

Ready for Phase 5: Blog List Management - CMS interface to view, search, and filter blog posts.

---
*Phase: 04-cms-foundation*
*Completed: [date]*
</output>
